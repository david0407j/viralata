version: "3.3"

services:
  database:
    container_name: viralata
    image: postgres
    restart: always
    volumes:
      - ./.pgdata:/var/lib/postgresql/data
    environment:
      - LC_ALL=C.UTF-8
      - POSTGRES_PASSWORD=davidson
      - POSTGRES_USER=davidson
      - EMAIL_PORT:5432:5432